// Inclut la bibliothèque standard pour les opérations d'entrée/sortie (printf/scanf)
#include <stdio.h>

// Fonction principale du programme - point d'entrée
// void = ne prend aucun paramètre
// int = retourne un entier (code de sortie du programme)
int main(void) {

    // Affiche la première ligne de texte avec un saut de ligne (\n)
    printf("Hello, I'm Zenos-Dev.\n");
    
    // Affiche la deuxième ligne de texte avec un saut de ligne
    printf("I'm a beginner in the world of software architecture.\n");
    
    // Affiche la troisième ligne de texte avec un saut de ligne
    printf("And I am very happy and enthusiastic to learn and grow in this field!\n");

    // Retourne 0 pour indiquer que le programme s'est exécuté avec succès
    return 0;
}